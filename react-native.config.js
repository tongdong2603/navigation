//config Fonts
/* va them package.json "rnpm": {
  "assets": [
    "./assets/fonts"
  ]
},*/
module.exports = {
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  assets: ['./assets/fonts/'], // stays the same
};
